/**
 * This file is used to import the different Counter examples.
 * Other files can then  `import { Counter } from "./counter.js"`
 * without worrying about updating the import when switching example.
 */

import { Counter } from "./counter-example1.js";

export { Counter };
